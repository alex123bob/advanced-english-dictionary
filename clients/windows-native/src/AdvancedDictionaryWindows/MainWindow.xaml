<Window
    x:Class="AdvancedDictionaryWindows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Advanced English Dictionary">
    <Grid Padding="28" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock Text="Advanced Dictionary" FontSize="34" FontWeight="Bold" />
            <TextBlock Text="Native Windows lookup using the shared dictionary API." Foreground="Gray" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="160" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" PlaceholderText="Search a word or phrase" KeyDown="SearchBox_KeyDown" />
            <Button Grid.Column="1" Content="Look Up" Click="SearchButton_Click" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="14">
                <TextBlock Text="API Base URL" FontWeight="SemiBold" />
                <TextBox x:Name="ApiBaseUrlBox" Text="http://localhost:8000" />
                <TextBlock Text="Recent" FontWeight="SemiBold" Margin="0,18,0,0" />
                <ListView x:Name="RecentList" SelectionChanged="RecentList_SelectionChanged" />
            </StackPanel>

            <ScrollViewer Grid.Column="1">
                <StackPanel x:Name="ResultPanel" Spacing="14">
                    <TextBlock x:Name="StatusText" Text="Start by searching a word." Foreground="Gray" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
